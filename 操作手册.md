# 网站内容更新手册

这份手册按**「你要做的事」**组织，不按文件组织。想查「某个内容在哪个文件」，看文末的速查表；想了解技术细节，看 `README.md`。

> 打开这份文件请用 **VS Code**（或 Windows 记事本也可以）。如果发现中文变成「缃戠珯」这种怪字，说明编辑器用错了编码（GBK）——换 VS Code 打开就正常，文件本身没坏。

---

## 0. 先记住三句话

1. **网站上的所有内容都是文件里的文字。** 改文件 = 改网站，没有后台、没有数据库。
2. **改坏了不会让网站白屏。** 万一写错导致构建失败，线上继续显示上一版，访客看不出来。你永远不会「把网站改垮」。
3. **提交之后大约 1 分钟上线。** 不用手动发布，不用上传，什么都不用点。

---

## 1. 第一次用之前：装两样东西

你现在电脑上**还没有 Git**，所以「推上去就发布」这条路暂时走不通。先把两样装好，一次装完以后就都是照着点。

| 要装什么 | 用来干嘛 | 下载地址 |
|---|---|---|
| **Node.js**（LTS 版） | 本地预览网站 | <https://nodejs.org> |
| **Git** | 把改动提交、推送上线 | <https://git-scm.com> |

两个都是一路点「Next」装完即可，不用改任何选项。

装完 Git 后，在 `su-site` 目录里执行一次初始化（只做一次）：

```bash
git init
git add .
git commit -m "初始版本"
```

之后再到 GitHub 上新建一个空仓库，按它给的提示把本地和远端连起来。做完这一次，以后就只用 `git push` 了。

> **不想敲命令就双击脚本**（都在 `su-site` 根目录）：
> - `1-初始化并提交.bat` —— 建本地仓库、提交第一版
> - `2-推送到GitHub.bat` —— 粘贴仓库地址，推上去
> - `3-日常更新推送.bat` —— 以后每次改完内容，双击它、写一句说明即发布
>
> 三个脚本都会自己找 Git（装在系统盘或 D 盘都能找到）。

> 在装好 Git 之前，你依然可以正常改文件、本地预览，只是改完没法推送上线。

---

## 2. 两种改法，选一种

### 方式 A：在 GitHub 网页上直接改（推荐日常用）

不用装任何软件，平板电脑上也能操作。

1. 打开你的仓库页面
2. 点进要改的文件，点右上角**铅笔图标**（Edit this file）
3. 改完，拉到页面最底部
4. 在 "Commit changes" 上方那个框里写一句说明，比如「新增三张生境照」
5. 点绿色的 **Commit changes**

**局限**：网页上改文字方便，但**没法上传图片**。所以凡是涉及图片的操作（加照片、文章配图），用方式 B。

### 方式 B：本地用 VS Code 改（加图片时用这个）

1. 打开 VS Code → File → Open Folder → 选 `su-site` 文件夹
2. 左侧文件树里找到要改的文件，点开
3. 改完 `Ctrl + S` 保存
4. 打开 VS Code 的终端（Terminal → New Terminal），依次输入：

```bash
git add .
git commit -m "新增三张生境照"
git push
```

**想要「先看见再发布」**：在终端输入 `npm run dev`，看到出现 `localhost:4321` 后，浏览器打开 <http://localhost:4321>。之后每保存一次文件，浏览器自动刷新，不用重启、不用刷新页面。

> **最省事的做法**：直接双击站点根目录的 `start-dev.bat`，它会自动配好环境、启动预览并打开浏览器，不需要敲命令。

- **网址用 `localhost`，别用 `127.0.0.1`**：`astro dev` 默认只监听 `localhost`，在 Windows 上它走的是 IPv6 的 `::1`；`127.0.0.1` 是 IPv4，两者不是同一个门牌号，输 `127.0.0.1:4321` 会显示「拒绝连接」。要用 `127.0.0.1` 就得写成 `npm run dev -- --host 127.0.0.1`（`start-dev.bat` 里已经这么写了）。
- **停止预览**：回到终端按 `Ctrl + C`（双击 bat 启动时，直接关掉那个黑窗口就行）
- **关掉之后想再看**：重新执行一次 `npm run dev`
- **别直接双击 `dist/` 里的 html 文件**：那些页面的资源路径是绝对路径，本地双击打开会是错版。要看效果就用 `npm run dev`

满意了再 `git push` 发布。

---

## 3. 任务一：添加新的摄影作品

三步：**放图片 → 登记信息 → 提交**。

### 第 1 步：把图片放进文件夹

把图片文件复制到 `src/assets/` 里。

**文件命名建议**：用英文小写 + 数字，不要有空格和中文，比如 `macro-jellyfish-01.jpg`。
（原因：部署服务器是 Linux，区分大小写；中文和空格在网址里会变成一长串乱码。）

**支持的格式**：`jpg` `jpeg` `png` `webp` `avif` `gif`，**大小写都认**——相机直出的 `IMG_1234.JPG` 直接丢进去就行。
不支持相机 RAW（`.cr2` `.nef` `.arw`）、TIFF、SVG，这些要先用看图软件导出成 JPG。

**体积**：构建时 Astro 会自动压成 WebP 并生成多个尺寸（缩略图 160/320/480，点开的大图 800/1200/1600），浏览器按屏幕挑一张下载。不管放什么格式进去，**网站上最终输出的一律是 WebP**。

话虽如此，**放进来之前最好先压一下**，标准是「长边 2400 像素、质量 82」——原因和具体数字见下面第 3.5 节。
嫌一张张压麻烦，就双击 `tools/压缩图片.bat`：它会自动把超标的图压到标准内，原图备份到 `_原始图备份/`（这个文件夹不上传 GitHub）。

**动图 GIF 也能用**，动画会保留（Astro 会转成动图 WebP，实测 128 帧进去 121 帧出来）。
站点已经自动把首屏动图限制在长边 1000、质量 62，首页那两张现在是 **1.2 MB 和 460 KB**——
**一般不用你手动处理**。只有单张超过 1.5 MB 时才需要管，做法见第 3.5 节。

**想分类整理**：`src/assets/` 下已经建好了 `macro/`、`micro/`、`habitat/` 三个文件夹，对应三个画廊。放进子文件夹的话，第 2 步的 `file` 要带上文件夹名：`file: 'macro/jellyfish-01.jpg'`。放根目录也完全没问题——**只是整理，不影响显示**。

### 第 2 步：在 `src/data/photos.ts` 里登记

打开 `src/data/photos.ts`，找到你要放的那个画廊（见下表），在它的 `plates: [...]` 里**复制一整段**、改掉里面的字：

```ts
{
  file: 'my-new-photo.jpg',                          // 第 1 步放进 assets 的文件名
  title: 'Salamandridae · 蝾螈',                      // 学名或俗名
  caption: 'Nikon Z7ii · 105mm · 浙江 · 2026-04',     // 图注，一句话；写拍摄参数也很好
  note: '点开大图后才显示的那段详细说明，可以不写',      // 可以整行删掉
  alt: '溪石下的蝾螈成体',                             // 给读屏软件和图片加载失败时用
},
```

| 字段 | 显示在哪 | 必填 |
|---|---|---|
| `file` | ——（决定显示哪张图） | 是 |
| `title` | 缩略图下方的标题 | 是 |
| `caption` | 缩略图和大图下方的说明句 | 是 |
| `alt` | 图片加载失败时显示 | 是 |
| `note` | **只有点开大图才显示**的详细说明 | 否 |

**三个画廊怎么分**：

| 画廊 | 放什么 |
|---|---|
| `macro` 宏观世界 | 肉眼可见的尺度：数码与胶片拍的形态、行为、质感 |
| `micro` 微观世界 | 显微物镜、焦点堆栈换来的结构细节 |
| `habitat` 自然生境 | 退后一步的生境照：林冠、草甸、溪流、湿地 |

> 提醒一句：摄影圈里的 "Macro" 常被理解成**微距**，而你这里指的是肉眼可见世界。如果访客里有摄影爱好者可能产生误解，可以把 `title` 改成 `Macro world · 肉眼可见的世界`。

### 第 3 步：提交

方式 A 或方式 B，见第 2 节。

## 3.5 图片压到多大合适（附一键工具）

**先说结论：卡帧的元凶通常不是文件体积，而是像素尺寸。**

举个例子：有一张扫描图是 14560×6796，压成 WebP 后「只有」3.7 MB，看着不算离谱——但浏览器显示它时要在内存里摊开约 9900 万像素（约 400 MB），幻灯片淡入淡出时必然掉帧。
把它缩到长边 2400 后，同一张图只有 327 KB，肉眼在网页上看不出区别。

所以判断标准是**长边像素**，不是 KB：

| 用在哪 | 长边建议 | 网页上单张体积 |
|---|---|---|
| 首屏轮播大图 | 1920–2560 | 200–500 KB |
| 点开看的大图 | 1600–2000 | 150–400 KB |
| 缩略图 | 不用管，Astro 自动生成 | 20–60 KB |
| **动图（GIF）** | 站点自动按长边 1000 输出 | 单张 ≤ 1.5 MB 就不用管 |

放进 `src/assets/` 的**原图**，统一按「长边 2400、质量 82」就够了。再大纯属浪费：网页上最大的显示宽度也就 1100–1600，乘 2 倍高清屏也才 3200。

### 静态图：一键压缩

双击 `tools/压缩图片.bat`。它会：把超标的图缩到长边 2400 并存成质量 82 的 JPEG/WebP；原图按目录结构备份到 `_原始图备份/`；已经达标的图直接跳过。
想改标准就编辑 `tools/compress-images.cjs` 开头的 `MAX_EDGE` 和 `QUALITY` 两行。

> PNG 压不动：PNG 是无损格式，缩小尺寸有效、调质量无效。截图类的 PNG 想变小，得在 ImageJ 里导出成 JPG 再放进来。

### 在 ImageJ 里手动压（如果你更习惯这个）

1. 打开图片 → `Image ▸ Scale…`：勾 `Constrain aspect ratio`，把长边那一栏填 **2400** → OK
   （想算缩放比例：2400 ÷ 原图长边，比如 14560 → 填 0.165）
2. `File ▸ Save As ▸ JPEG…`：质量填 **80–85**

批量：`Process ▸ Batch ▸ Convert…`，选输入/输出文件夹，Output 格式选 JPEG，`Scale factor` 填算出来的比例。

### 动图：一般不用管，超了再处理

实测结论：**动图真正的体积杀手是格式，不是帧数。**
同一张 128 帧的图，源 GIF 是 6.5 MB，转成动图 WebP 只剩 1.2 MB——差 5 倍；
而抽掉一半帧只能再省不到 20%，还会让动作变生硬。
所以站点已经替你做了最有效的一步：首屏动图统一输出长边 1000、质量 62
（在 `src/pages/index.astro` 里搜 `isAnim` 就能看到）。

**什么时候才需要动手**：单张动图在网页上超过 1.5 MB，或帧数超过 300 帧（老机器解码会吃力）。这时两头各削一点：

- **缩尺寸**：长边降到 700–800
- **抽帧**：每 2 帧丢 1 帧，12–15 fps 足够流畅

ImageJ 做法：打开 GIF 会变成多帧的 stack → `Image ▸ Scale…` 勾 `Process entire stack` 缩放
→ `Image ▸ Duplicate…` 的 Range 填 `1-125-2`（每隔一帧取一张）→ `File ▸ Save As ▸ Animated GIF…`。

有 ffmpeg 的话一条命令更快（还能直接转成体积只有 GIF 十分之一的 MP4）：

```
ffmpeg -i heart.gif -vf "scale=800:-1,fps=12" heart-small.gif
```

首页那两张动图现在合计 1.7 MB，首屏总下载 3.0 MB（优化前是 4.3 MB），已经不卡了。

### 顺带：删照片 / 换画廊 / 换图

- **删**：把那一整段（从 `{` 到 `},`）删掉
- **挪到别的画廊**：整段剪切，粘贴到另一个画廊的 `plates` 里
- **换图**：新图丢进 `assets/`，把 `file` 改成新文件名
- **加第四个画廊**：复制整个 `{ id: 'macro', ... }` 段，改掉 `id`（英文，不能和已有的重复）和标题

---

## 4. 任务二：发布一篇新文章

文章都在 `src/content/posts/` 里，一个文件一篇。**文件名决定网址**：`no-single-best-tree.md` → 网址是 `/posts/no-single-best-tree`。

两条路，看你从哪出发：直接在电脑上写 Markdown，或者**从 Word 一键转换**（推荐，格式保真）。

### 路线 A：从 Word 一键转换（推荐）

你在 Word 里排版好的文章，可以连带字号、颜色、加粗、倾斜、下划线和图片一起搬上来，不用重新排版。

转换程序在：`C:\Users\Administrator\WorkBuddy\Academicabin推文\docx2publish.py`

在终端里执行（把文件名换成你的）：

```bash
cd C:\Users\Administrator\WorkBuddy\Academicabin推文
python docx2publish.py "文章.docx" --site "C:\Users\Administrator\WorkBuddy\2026-09-19-19-52-34\su-site" --tag 科普 --min-size 12
```

一次生成**两份东西**：

1. **网站用的 Markdown**——自动放进 `src/content/posts/`，图片自动落到 `src/assets/posts/`，直接就能构建
2. **`文章_公众号.html`**——用浏览器打开，点「一键复制」，到公众号后台 `Ctrl + V`，格式和图片全都在（图片是内嵌的，不用再单独传图）

| 参数 | 作用 |
|---|---|
| `--site <路径>` | 直接发到网站里。不给就只生成文件，你自己拷 |
| `--tag 科普` | 文章标签，不给默认是「未分类」 |
| `--min-size 12` | 小于 12px 的字强制抬到 12px（Word 里的小字在手机上会太小） |
| `--scale 1.15` | 全部字号放大 15% |
| `--no-html` | 只要 Markdown，不生成公众号版 |

跑完程序会在最后打一份**「体检报告」**：字号分布、图片数量、加粗斜体统计，还会提醒你哪些段落疑似小标题但没设成标题样式。看一眼再决定要不要加 `--min-size` 重跑。

> **两件事要注意**：
> 1. 程序生成的 `.md` 文件名沿用 Word 文件名。如果 Word 文件名是中文（比如现在那篇「让断指实现再生的神奇粉末…」），**建议手动改成英文**，否则网址会是一长串编码。改名不会影响文章标题，标题在文件里的 `title:` 那一行。
> 2. Word 里的表格、脚注、公式目前不转，遇到就手动补一下。

### 路线 B：直接写 Markdown

在 `src/content/posts/` 新建 `my-new-post.md`，开头这样写：

```
---
title: 文章标题
date: 2026-09-20
tag: 方法学
summary: 列表页上显示的那句话。
---

正文从这里开始。用 Markdown 写：
## 这是二级标题
**这是加粗**
```

开头 `---` 之间那块叫 frontmatter，是文章的信息卡。**列表页显示的标题、日期、标签、摘要都从这里读**，不是从正文里的一级标题读。

**最容易写错的三点**：

1. 冒号后面必须有一个空格：`title: xxx` 对，`title:xxx` 错
2. 日期写成 `2026-09-20`，不要加引号
3. 四项（title / date / tag / summary）缺一项会导致**构建失败**

**不想发布某篇**：在 frontmatter 里加一行 `draft: true`。

**正文里插图片**：图片放 `src/assets/posts/`，正文里写 `![说明](../../assets/posts/xxx.jpg)`。往上退两级 `../../` 不能少——文章在 `src/content/posts/`，要退两级才回到 `src/`。

---

## 5. 任务三：更新工具链接

打开 `src/data/tools.ts`，每个链接三项：

```ts
{
  name: 'NCBI BLAST',
  url: 'https://blast.ncbi.nlm.nih.gov/Blast.cgi',
  note: '同源序列检索，确认片段身份的第一道关。',
},
```

- `url` 必须是完整地址，带 `https://`
- 加一个链接：复制一整段改掉三项即可
- 加一个新分组：复制整个 `group` 段改名字

---

## 6. 任务四：改 About me

### 改简历内容

打开 `src/data/cv.ts`：

- `contact` 是左栏联系信息。`href` 写成 `mailto:xxx@xxx.com` 就是邮件链接；留空字符串 `''` 就不做成链接（比如语言、时区那些项）
- `cvSections` 是右栏，现在是「教育 / 研究经历 / 发表选录」三组
  - 加一段经历 = 复制 `items` 里的一整段
  - 加一个分组 = 复制整个 `cvSections` 里的一段

**简历 PDF**：把 `cv.pdf` 放进 `public/` 目录，链接就自动生效。

### 改 About me 的肖像照

1. 把照片放进 `src/assets/`（文件名用英文小写，比如 `portrait.jpg`）
2. 打开 `src/site.ts`，找到 `about: {` 这一段，改这两行：

```ts
portrait: 'portrait.jpg',        // 换成你放进去的文件名
portraitAlt: '野外的研究者',      // 图片加载失败时显示的文字，也给读屏软件用
```

3. 保存、提交

**尺寸提示**：这块图显示为宽 340px、高 400px 的框（手机上是 320px），图片**居中裁切成 5:4**。所以**竖构图最合适**——横着拍的照片会被裁掉左右两边，人可能只剩半张脸。

> 文件名对不上会显示「缺图：xxx」灰块。现在加了一层容错：**扩展名写错也能找到**（数据里写 `.png`、实际文件是 `.jpg`，照样显示），但文件名主体必须一致。

### About 的英文版

英文版在 `src/data/en.ts`，页面地址是 `/en/about`，中文页右上角有切换链接。

**英文版不会自动跟随中文更新**——你改了 `cv.ts` 的中文内容，要手动去 `en.ts` 里把对应那句英文也改掉，否则两边会不一致。

---

## 7. 想改网站上的其他文字

都在 `src/site.ts`：

| 想改什么 | 找哪个字段 |
|---|---|
| 网站标题、页脚 | `site.ts` 顶部 |
| 顶部导航 | `nav` 数组（加一项就复制一行，`href` 要和 `src/pages/` 下的文件名对应） |
| 首页大标题和自我介绍 | `home` 这一段 |
| 首屏轮播图 | `home.heroSlides`（每行一个文件名；只留一张就不轮播） |
| 首页 4 张导览卡 | `src/data/sections.ts` |

**轮播速度**：打开 `src/pages/index.astro`，最上面有 `SLIDE_SEC`（每张停留秒数）和 `FADE_SEC`（淡入淡出秒数），改数字即可。
**只有横构图**（宽明显大于高）的照片适合放首屏，竖图会被裁掉大半。

### 给文字排版：换行、斜体、加粗

上面表格里的所有文字字段（`site.ts`、`photos.ts`、`tools.ts`、`cv.ts`、`en.ts`）都支持三种写法，在引号里直接写符号：

| 写法 | 效果 | 例子 |
|---|---|---|
| `\n` | 换行 | `'第一行\n第二行'` |
| `*文字*` | 斜体 | `'*Branchiostoma floridae* · 文昌鱼'` |
| `**文字**` | 加粗 | `'**重点**：这一句要强调'` |

- **物种拉丁名按学术惯例要斜体**，加一对星号即可，摄影页的图注、大图说明都认。已有的 `Branchiostoma floridae`、`Carabidae` 等都按这个写法排好了
- 单个星号（比如乘号）不会被误认，必须成对出现才生效
- `\n` 只在真正显示到页面上的文字里生效；浏览器标签页标题、搜索摘要这些地方会自动压成空格，不用管
- 文章正文（`.md`）是 Markdown，天然支持这些格式，不需要星号简写

### 分页

两个列表页自带分页，**不需要你做任何事**：内容超过一页时，页码栏自动出现在右下角；不满一页时自动隐藏。

| 页面 | 每页数量 | 在哪里改 |
|---|---|---|
| Photography（每个画廊独立翻页） | 6 张缩略图 | `src/pages/photography.astro` 顶部的 `PER_PAGE` |
| Academicabin | 10 篇文章 | `src/pages/academicabin.astro` 顶部的 `PER_PAGE` |

翻页是纯 CSS 实现的（零 JavaScript）：所有内容都已在页面里，页码只是切换显示，所以网址不变、不刷新、搜索引擎照常收录全部内容。加载新照片或新文章后页数自动变化，无需手动维护页码。

---

## 8. 出问题怎么办

| 现象 | 原因与处理 |
|---|---|
| 线上没变化 | 先按 `Ctrl + F5`（Mac `Cmd + Shift + R`）硬刷新。十次有九次是缓存 |
| 收到「构建失败」邮件 | 多半是 `.md` 的 frontmatter 写错了。**线上仍是上一版，访客看不出问题**。改回去再提交一次 |
| 图片位置显示「缺图：xxx」 | 文件名和 `src/assets/` 里的实际文件对不上。查大小写、扩展名、子文件夹名 |
| 改错了想退回去 | GitHub 仓库页面点 Commits，找到之前那次，一键回退 |
| 本地跑不起来 | 先确认装了 Node.js，然后在 `su-site` 目录下重新执行 `npm install` |
| 中文变成怪字 | 文件本身没坏，是编辑器用 GBK 打开的。换 VS Code 打开 |

---

## 9. 速查表：改 X 去哪里

| 内容 | 文件 |
|---|---|
| 全站文字设定（标题、导航、页脚） | `src/site.ts` |
| 首页导览卡 | `src/data/sections.ts` |
| 摄影作品 | `src/data/photos.ts` |
| 文章 | `src/content/posts/*.md` |
| 工具链接 | `src/data/tools.ts` |
| About me 中文 | `src/data/cv.ts` |
| About me 英文 | `src/data/en.ts` |
| 图片文件 | `src/assets/`（放这里会自动压缩） |
| 不需要压缩的文件（PDF 等） | `public/` |
| Word 转文章 | `Academicabin推文\docx2publish.py` |

**`src/pages/`、`src/components/`、`src/layouts/` 是版式，不是内容。**
日常更新不用碰它们。那是管「长什么样」的地方，不是管「写什么」的地方。

---

## 10. 日常命令（方式 B 用）

在 VS Code 终端里，`su-site` 目录下执行：

```bash
npm install   # 第一次拿到项目时执行一次
npm run dev   # 本地预览，边改边看（Ctrl+C 停止）
npm run build # 生成 dist/ 静态文件（部署平台会自动做，平时不用手动跑）
git add .     # 下面三条是推送上线
git commit -m "说明文字"
git push
```

> 不想敲命令的话，双击 `3-日常更新推送.bat` 就等价于上面最后三条。
