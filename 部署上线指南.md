# 上线指南：GitHub Pages（主方案）+ Cloudflare Pages / 国内平台（备选）+ 腾讯云域名

这份文档只讲「怎么把网站放到网上」，日常改内容看 `操作手册.md`。

主方案是 GitHub Pages（免备案、当天上线）；文末两节分别是 Cloudflare Pages 和国内平台（需 ICP 备案）。三者可以连同一个 GitHub 仓库并存，最后决定域名指向谁即可。

顺序是：**装 Git → 建仓库 → 推送 → 开启 Pages → 买域名 → 解析 → 绑定**。
前四步做完，网站就已经能用 GitHub 送的免费网址访问了；后面三步是换成你自己的域名。

> **关于备案**：GitHub Pages 的服务器在境外，走这条路**不需要 ICP 备案**。
> 但域名本身要在腾讯云做**实名认证**（上传身份证，一般几小时内通过）——实名和备案是两回事，别混淆。

---

## 第 0 步：我这边已经备好的

以下文件我已经放进项目里，你不用管：

| 文件 | 作用 |
|---|---|
| `.github/workflows/deploy.yml` | 自动构建发布的工作流。每次推送代码，GitHub 自动帮你构建、上线 |
| `.gitignore` | 已排除 `node_modules/`、`dist/` 和本地调试脚本 |
| `public/.nojekyll` | 防止 GitHub 用 Jekyll 二次处理，导致 `_astro` 目录被忽略 |
| `.gitattributes` | 统一换行规则：仓库里存 LF，`.bat` 保持 CRLF，避免自动化脚本在服务器上出错 |

本地仓库已经建好，两次提交的作者均为 `Su-bot-cc <673910907@qq.com>`，这一步不用你再做。

**等你确定域名后**，我再帮你改两处：`astro.config.mjs` 里的 `site`（填正式网址）和 `public/CNAME`（填域名，防止每次部署把域名设置冲掉）。

---

## 第 1 步：安装 Git

打开 <https://git-scm.com/download/win> ，下载 64-bit 安装包，一路点 Next 用默认选项装完即可。

> 注意：下载页面有时会自动跳转到「安装」，也可能只是下载了安装包——**必须真正运行安装包走完安装流程**才算装好。

装完验证：打开「命令提示符」，输入 `git --version`，能显示 `git version 2.x.x` 就成了。

> 装好后**关掉重开一次**命令提示符，否则新装的 Git 还不在环境变量里。

## 第 2 步：在 GitHub 上建仓库

1. 登录 GitHub，右上角 `+` → **New repository**
2. Repository name 填 **`su-bot-cc.github.io`**——**必须和用户名一模一样（小写）**
3. 选 **Public**（GitHub Pages 免费版只对公开仓库开放；私人仓库要付费）
4. **不要勾** "Add a README file"、"Add .gitignore"、"Choose a license"——本地已经有内容了，勾了会冲突
5. 点 **Create repository**

> 为什么不是 `su-site`？只有「仓库名 = 用户名.github.io」时，GitHub 才会把站点放在**根路径**（`https://su-bot-cc.github.io/`）。
> 换成别的名字，网址会多一级 `/su-site/`，站内所有链接都要跟着改前缀，将来绑自己的域名也容易踩坑。
> 代价只是仓库名长得像网址——一个账号只能有一个这样的仓库，个人主页正合适。

建好后页面会显示仓库地址：
`https://github.com/Su-bot-cc/su-bot-cc.github.io.git`
`2-推送到GitHub.bat` 已经预填了这个地址，双击后直接回车即可。

> ⚠️ 仓库是公开的，所以别把身份证、密钥、未发表的原始数据放进这个文件夹。

## 第 3 步：把本地代码推上去

**不想敲命令的话，双击 `1-初始化并提交.bat`**（在站点根目录里），它会自己完成 init、配置身份、提交；建好 GitHub 仓库后再双击 `2-推送到GitHub.bat`，粘贴仓库地址回车即可。

想手动执行的话，打开命令提示符逐行执行（把地址换成你自己的仓库）：

```bash
cd /d C:\Users\Administrator\WorkBuddy\2026-09-19-19-52-34\su-site

git config user.name "Su-bot-cc"
git config user.email "673910907@qq.com"
git init
git add .
git commit -m "初始提交：学术主页"
git branch -M main
git remote add origin https://github.com/Su-bot-cc/su-bot-cc.github.io.git
git push -u origin main
```

第一次 `git push` 会弹窗让你登录 GitHub，按提示用浏览器授权即可（如果开了双重验证，需要用 Personal Access Token 代替密码——网页上会有引导）。

> 仓库里的图片资源合计约 220 MB（最大的一张 44 MB 的动图），第一次推送会比较慢，等几分钟是正常的。GitHub 单文件上限 100 MB、仓库建议控制在 1 GB 以内，目前都还在安全范围里；哪天想瘦身，优先压 `src/assets/heart.gif` 和 `amphioxus.png` 这两个。

## 第 4 步：开启 GitHub Pages

1. 打开仓库页面 → **Settings** → 左侧 **Pages**
2. **Build and deployment** → Source 选 **GitHub Actions**（不要选 "Deploy from a branch"）
3. 切到仓库的 **Actions** 标签页，会看到一次正在跑的构建任务（就是刚才推送触发的）
4. 等它变绿（约 1–2 分钟），点进去能看到部署结果和网址

此时网址是：`https://su-bot-cc.github.io/`

先打开确认能正常显示。因为站点在根路径，将来绑了自己的域名后，访问这个旧地址会自动跳转到新域名，不会失效。

## 第 5 步：买域名 + 实名认证

腾讯云搜索「域名注册」，挑一个买下。

选域名的一点建议（你是个人学术主页）：

- 后缀：`.com` 最通用、最容易被当真；`.site`、`.me` 便宜且适合个人；`.cn` 便宜但要实名更严，且将来若迁国内服务器才划算
- 名字：短、好拼、听得懂。姓氏全拼 + 短后缀是不错的选择，避免连字符和数字
- 价格：`.com` 首年常有三四十的活动，续费通常在 60–90 元/年

买完后在腾讯云控制台做**域名实名认证**（上传身份证正反面，个人认证），一般几小时内通过。**认证通过后 DNS 解析才会生效**。

## 第 6 步：配置 DNS 解析

在腾讯云控制台进入「云解析 DNS」，找到你的域名，添加以下记录：

| 主机记录 | 记录类型 | 记录值 |
|---|---|---|
| `@` | A | `185.199.108.153` |
| `@` | A | `185.199.109.153` |
| `@` | A | `185.199.110.153` |
| `@` | A | `185.199.111.153` |
| `www` | CNAME | `su-bot-cc.github.io` |

前四条是让「不带 www 的主域名」指向 GitHub，第五条让 `www.` 也指向同一个站。

TTL 保持默认（600 秒）即可。解析生效通常需要 10 分钟到 2 小时，极少数要等 24 小时。

## 第 7 步：在 GitHub 绑定域名

1. 仓库 → **Settings** → **Pages** → **Custom domain**
2. 填入你的域名（例如 `liushu.site`，不带 `https://`、不带 www）
3. 点 **Save**，GitHub 会做一次 DNS 校验，通过后会显示 "DNS check successful"
4. 勾上 **Enforce HTTPS**（证书由 GitHub 自动签发，可能要等几分钟到几十分钟才出现）

同时告诉我你的域名，我来补 `astro.config.mjs` 的 `site` 和 `public/CNAME` 两个文件——**这一步不做的话，每次自动部署都会把你在网页上填的域名清掉**。

## 第 8 步：以后怎么更新

改完任何文件后，命令提示符里三行：

```bash
cd /d C:\Users\Administrator\WorkBuddy\2026-09-19-19-52-34\su-site
git add .
git commit -m "改了点什么"
git push
```

推送完 1–2 分钟，网站自动更新。也可以在 GitHub 的 Actions 页面看构建日志，失败会标红。

---

## 备选方案：改用 Cloudflare Pages

如果不想用 GitHub Pages，可以换成 Cloudflare Pages。它和上面的流程**不冲突**——两个平台可以连同一个 GitHub 仓库各自构建，你最后决定把域名指向谁就行。

**和 GitHub Pages 的差别**：

| | GitHub Pages | Cloudflare Pages |
|---|---|---|
| 费用 | 免费 | 免费 |
| 带宽 | 每月 100GB 软上限 | 不限（合理使用范围内） |
| 构建速度 | 1–2 分钟 | 通常 1 分钟内 |
| 国内访问 | 偏慢，偶发不稳 | 略好一些，但同样偶发不稳 |
| 额外账号 | 不需要 | 需要注册 Cloudflare |
| 分支预览 | 无 | 每次推送自动生成预览网址 |

对你这种图片较多的站点，**不限带宽**是最实在的优势。

### 操作步骤

**1. 注册 Cloudflare**
打开 <https://dash.cloudflare.com/sign-up> ，用邮箱注册免费账号。

**2. 连接 GitHub 仓库**
左侧 **Workers & Pages** → **Create** → **Pages** → **Connect to Git** → 授权 GitHub → 选中 `su-bot-cc.github.io` 仓库 → 点 **Begin setup**。

**3. 填写构建配置**（这一步是关键）

| 字段 | 填什么 |
|---|---|
| Project name | `su-bot-cc`（随意，会成为 `xxx.pages.dev` 的一部分） |
| Production branch | `main` |
| Framework preset | `Astro`（选了会自动填下面的命令） |
| Build command | `npm run build` |
| Build output directory | `dist` |

**4. 设置 Node 版本（重要）**
展开 **Environment variables (advanced)**，添加一条：
- 变量名：`NODE_VERSION`
- 值：`22`

不设的话 Cloudflare 可能用较旧的 Node 构建，Astro 7 会报错。项目里我已经放了 `.nvmrc`（内容 `22`）作为双保险。

**5. 部署**
点 **Save and Deploy**，等一分钟左右，会得到 `https://su-bot-cc-xxx.pages.dev`，先打开确认显示正常。

**6. 绑定自己的域名**
项目页 → **Custom domains** → **Set up a custom domain** → 填入你的域名。

Cloudflare 会给出需要添加的 DNS 记录，按提示去腾讯云「云解析 DNS」添加即可（一般是把 `@` 做 CNAME 指到 `su-bot-cc-xxx.pages.dev`，再加一条 TXT 做所有权验证）。验证通过后 HTTPS 证书自动签发，等几分钟到几十分钟。

> 另一种做法是把域名的 DNS 服务器整个转到 Cloudflare（改 NS 记录），之后解析都在 Cloudflare 里管理，还能开它的 CDN 加速。但改动大、生效慢（最长 48 小时），第一次上线建议先用 CNAME 这种轻量方式。

**7. 日常更新**
和 GitHub Pages 一样：本地 `git add .` → `commit` → `push`，Cloudflare 自动构建发布。以后换不换平台都不影响本地这套操作。

---

## 备选方案：国内平台（要备案，但国内打开最快）

只要服务器在中国大陆，**就必须完成 ICP 备案**，这是硬性要求，没有绕过办法。

| | 境外平台（GitHub / Cloudflare） | 国内平台 |
|---|---|---|
| ICP 备案 | 不需要 | **必须**，7–20 个工作日 |
| 国内打开速度 | 偏慢、偶发不稳 | 快且稳定 |
| 上线时间 | 当天 | 备案下来后 |
| 成本 | 0 | 几元～几十元/月 |
| HTTPS 证书 | 平台自动签发 | 平台免费证书，需自己申请一次 |

### EdgeOne Pages 到底收不收费（2026-09 核实）

官方 FAQ 与定价页写明：**免费套餐长期提供，控制台一键开通**，核心权益如下：

| 项目 | 免费额度 |
|---|---|
| 安全加速流量 | **不限** |
| 安全加速请求数 | **不限** |
| 每月构建次数 | 500 次 |
| Edge Functions | 300 万次请求 / 月 |
| Cloud Functions | 100 万次请求、50 万 GBs / 月 |
| KV 存储 | 1 GB |

官方原话是「免费版本将永久提供」，以及「在推出收费版之前，即使超出免费版的用量限制也不会中断服务」。

开通只需要腾讯云账号实名 + Pages 控制台一键开通，**不用先买 EdgeOne 套餐**。限额主要落在构建次数（一天推三次都用不完）和边缘函数（静态站用不到）。

对你这种图片占大头、单页面几 MB 的站点，**流量和请求数不限**才是最值钱的一条——同样的带宽在别家是主要卖点，在这里是白送的。

### 平台怎么选

- **腾讯云 EdgeOne Pages**（首选）：Git 导入自动构建，流量请求双不限，域名和解析都在腾讯云，备案最省事
- **腾讯云 CloudBase 静态网站托管**：更成熟，按量计费，有免费额度
- **腾讯云 COS 对象存储 + CDN**：最便宜，但要自己配 CDN、缓存、HTTPS，配置最繁琐

个人主页流量很小，三家实际花费都在每月几元到几十元。图省事就选 EdgeOne Pages。

> ⚠️ **一个容易忽略的前提**：EdgeOne Pages 建项目时要选**加速区域**。
> 「全球（含中国大陆）」必须域名已备案，国内延迟可到几十毫秒——这才是选它的理由；
> 「全球（不含中国大陆）」免备案，但国内流量走海外节点，速度优势几乎消失，甚至不如 Cloudflare 稳。
> 换句话说：**没备案之前，它不是一个提速方案，只是一个备用节点。**

### 操作步骤

**1. 先买域名并做实名认证**
域名实名认证通过之前，无法提交备案。

**2. 提交 ICP 备案**（腾讯云控制台 → 备案）

准备：身份证、手机号、域名证书。

几个容易卡住的点：
- **短信核验**：管局在审核中会发一条验证码短信，**必须在规定时间内回复**，超时整个备案作废重来。这是最常见的失败原因
- **网站名称**：填中性的，比如「个人研究主页」「学习笔记」，不要出现新闻、论坛、博客、行业机构等字样；具体以备案页面校验提示为准
- **备案期间网站不能对外访问**：未备案域名不允许解析到大陆服务器

**3. 备案期间，先用境外平台上线**（强烈建议）
备案要等一两周，这段时间域名**可以先指向 GitHub Pages 或 Cloudflare Pages**——解析到境外服务器不需要备案，两边不冲突。
这样你当天就有能访问的网站，等备案下来再把 DNS 改指到国内，几分钟生效。

**4. 在 EdgeOne Pages 建项目**（这一步依赖第 3 步：GitHub 仓库已经推上去）

1. 腾讯云控制台 → **EdgeOne** → 左侧 **Pages** → **新建项目**
2. 代码来源选 **GitHub**，首次会跳授权页，勾选 `su-bot-cc.github.io` 仓库
3. 构建配置：

   | 字段 | 填什么 |
   |---|---|
   | 框架预设 | 无（有 Astro 选项就选 Astro） |
   | 构建命令 | `npm run build` |
   | 输出目录 | `dist` |
   | Node 版本 | 在环境变量里加 `NODE_VERSION = 22`，不设可能构建失败 |

4. **加速区域**先选「全球（不含中国大陆）」（此时域名还没备案）；改天备案通过，再回项目设置里切成「含中国大陆」
5. 点部署，等 1–3 分钟，先用它给的临时预览地址验收

**5. 绑定域名**
项目 → **自定义域名** → 添加你的域名，控制台会给出一条 CNAME 目标；去腾讯云「云解析 DNS」，把原`@` 和 `www` 记录改成指向它。SSL 证书平台自动签发，不用自己去申请 DV 证书。

> 同一个域名同时挂在 GitHub Pages 和 EdgeOne Pages 上不冲突——**DNS 记录只有一条在生效**，指谁就用谁。
> 所以切换平台只是改一条解析记录，网站内容和本地那套 git 操作一行都不用动。

### 我的建议

**双轨走，不耽误事**：
1. 现在先用 GitHub Pages 上线，域名立刻能用
2. 同时把 EdgeOne Pages 也接上（同一个 GitHub 仓库，各自构建），作为国内备案后的落点
3. 备案通过后，只改一条 DNS 记录就切到国内节点，网站内容、配置一行都不用动

静态站的迁移成本几乎为零，所以先上线再切换是最划算的顺序。

---

## 几个提醒

- **国内访问速度**：GitHub Pages 的服务器在境外，国内打开有时偏慢或偶发抽风，这是选择它就要接受的代价。如果以后觉得不能忍，可以再迁到腾讯云 EdgeOne Pages（国内快，但要备案），静态站迁移不难。
- **仓库必须公开**：免费 Pages 的限制。
- **容量软限制**：仓库建议控制在 1GB 内、每月流量 100GB 内（个人主页远远够）。现在首屏几张动图加起来约 8MB，建议有空压缩一下。
- **不要在网页上直接改文件**：GitHub 网页端改了文件，本地仓库就和线上不一致了，下次推送会冲突。要么本地改，要么网页改完本地先 `git pull`。
- **域名记得续费**：到期不续会被抢注。开启自动续费最省心。
